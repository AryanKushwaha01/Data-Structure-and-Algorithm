int findDuplicate(vector<int>& nums) {
        int n = nums.size();
        int ans = 0;
        unordered_map<int, int> m;

        for(int x : nums){
            m[x]++;
            if(m[x] > 1){
                ans = x;
            }
        }
        return ans;
    }