int singleNumber(vector<int>& nums) {
        // map<int, int> mp;
        // for(int i : nums){
        //     mp[i]++;
        // }
        // for(int i = 0; i < nums.size(); i++){
        //     if(mp[nums[i]] == 1){
        //         return {nums[i]};
        //     }
        // }
        // return {-1};

	//2nd Method
        int ans = 0;
        for(int i : nums){
            ans = ans ^ i;
        }
        return ans;
    }